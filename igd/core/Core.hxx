#pragma once

#include <gum/Core.h>
#include <gum/Types.h>

namespace igd {

static constexpr auto null = gum::null;

using u8 = gum::u8;
using s8 = gum::s8;

using u16 = gum::u16;
using s16 = gum::s16;

using u32 = gum::u32;
using s32 = gum::s32;

using u64 = gum::u64;
using s64 = gum::s64;
}
